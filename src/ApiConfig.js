// 🔑 우리집 열쇠 보관함 (Key Config)

// 1. 미세먼지용 키
export const DUST_KEY = import.meta.env.VITE_FESTIVAL_KEY;
// 2. 약국용 키 (만약 미세먼지랑 똑같으면 그냥 똑같이 붙여넣으면 됨!)
export const HEALTH_KEY = import.meta.env.VITE_FESTIVAL_KEY;
export const WE_ARE_READY = true;